{
  "description": "rx-nostr's workspace root. This is private.",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w packages/core",
    "lint": "npm run lint -w packages/core",
    "test": "npm run test -w packages/core",
    "build": "npm run build -w packages/core",
    "docs:dev": "npm run dev -w packages/docs",
    "docs:build": "npm run build -w packages/docs"
  },
  "author": "penpenpng",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.56.0",
    "@typescript-eslint/parser": "^5.56.0",
    "eslint": "^8.45.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "prettier": "^3.2.5"
  }
}
