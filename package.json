{
  "private": true,
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "test": "npm run test -w packages/rx-nostr",
    "build": "npm run build -w packages/rx-nostr -w packages/crypto",
    "docs:dev": "npm run dev -w packages/docs",
    "docs:build": "npm run build -w packages/docs"
  },
  "author": "penpenpng",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^8.57.1",
    "globals": "^16.0.0",
    "typescript-eslint": "^8.29.1"
  }
}
